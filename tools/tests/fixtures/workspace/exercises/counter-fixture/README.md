# Admission counter fixture

Framework verification only; this is not a lesson or a calibration assignment.

Implement `admit(requested, capacity)` in `src/counter.cpp`. Return the smaller value.
Zero capacity admits nothing. Both inputs are non-negative `std::size_t` values.
Do not change the header, tests, or specification.
